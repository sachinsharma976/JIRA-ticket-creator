generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// status is a plain String ("PENDING" | "CREATED" | "FAILED"), validated in
// TypeScript. Postgres supports real enums, but this was carried over
// unchanged from an earlier SQLite version — fine to switch to a native
// enum later, not required for correctness today.
model Ticket {
  id                String   @id @default(cuid())
  status            String   @default("PENDING")
  jiraKey           String?  @unique
  jiraUrl           String?
  title             String
  issueType         String
  assigneeAccountId String?
  assigneeName      String?
  startDate         String?
  dueDate           String?
  errorMessage      String?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  createdBy         String?

  @@index([createdAt])
}
